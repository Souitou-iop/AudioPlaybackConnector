# AudioPlaybackConnector 更新日志 (Changelog)

## [v1.0-beta4] - 2026-08-23

### 🎨 浅色模式视觉修复与实时系统主题跟随
* **浅色模式设备容量角标反色修复**：彻底修复浅色模式下顶部设备数量指示角标（如 `[0/2]`、`[1/2]`）因白色文字在浅色背景下看不清的视觉缺陷，根据当前系统主题自适应高对比度前景色。
* **星标与状态文本深浅色适配**：已连接状态文本与未星标图标根据浅色/深色主题动态计算透明度与对比度，全天候清晰易读。
* **全动态实时系统深浅色切换 (Real-Time Theme Sync)**：接入 Windows `WM_SETTINGCHANGE` 与 `WM_THEMECHANGED` 消息，动态调整 XAML Islands 根容器、Fluent 面板与右键菜单的 `RequestedTheme`，系统切换深浅色时秒级响应，无需重启软件。

---

## [v1.0-beta3] - 2026-08-23

### 🌐 多语言与国际化
* **托盘右键语言切换子菜单**：在托盘右键「设置」中新增 **「语言 (Language)」** 二级菜单，支持 6 种语言选项：
  * `跟随系统 (System Default)`（默认）
  * `简体中文 (zh-CN)`
  * `繁體中文 (zh-TW)`
  * `English (en-US)`
  * `日本語 (ja-JP)`
  * `한국어 (ko-KR)`
* **免重启热重载 (Hot-Reload)**：切换语言后内存中即时清空旧缓存并重载新语言二进制表，托盘菜单、悬浮提示框及 Fluent 面板瞬间刷新生效。
* **语言配置持久化**：语言偏好自动保存至 `AudioPlaybackConnector.json` (`"language"`)。
* **全新日语与韩语支持**：编写并编译 `ja_JP.po` 与 `ko_KR.po`，覆盖 60+ 核心词条。

---

## [v1.0-beta2] - 2026-08-23

### 🚀 核心功能大升级
* **AAC / SBC 编解码格式状态标签**：智能识别当前蓝牙连接的音频 Codec，并在设备卡片标题旁呈现高亮 `[AAC]`（高清音频）或 `[SBC]` 标签。
* **一键唤起系统蓝牙配对向导**：设备列表为空时展示友好的配对引导卡片与 `[ ＋ 配对新设备 ]` 按钮，点击一键唤起 `ms-settings:bluetooth`。
* **CLI 扩展状态输出 (`/Status`)**：执行 `AudioPlaybackConnector64.exe /Status` 毫秒级向终端输出包含连接状态、设备列表、电量、音量、活跃状态的结构化 JSON 数据。
* **可选轻量系统气泡通知**：设备连接或断开时弹出原生托盘通知气泡，设置菜单中提供独立开关（默认开启）。
* **软件内置一键「检查更新」**：托盘右键新增 `🔄 检查更新` 选项，异步查询 GitHub Releases 并提示更新。

---

## [v1.0-beta] - 2026-08-23

### 🎛️ 初始重构与现代化版本
* **原生 Fluent 风格亚克力设备管理面板**：自适应暗黑模式、精致卡片、平滑阴影与右上角专属关闭按钮。
* **音频通道自动重路由**：基于 CoreAudio `IMMNotificationClient` 实时监听默认声卡切换并静默重新绑定，彻底解决连接后无声问题。
* **星标首选常用设备**：支持卡片一键星标置顶，双击托盘与快捷指令绝对优先快连。
* **实时电池电量百分比**：智能抓取蓝牙设备剩余电量并在卡片紧凑展示。
* **音频活跃电平指示**：基于 WASAPI 低频心跳电平检测，动态展示 `● 正在播放` 状态。
* **单实例互斥与 CLI 自动化控制**：支持 `/Show`, `/Toggle`, `/Connect`, `/Disconnect`, `/Exit` 命令行指令。
* **开箱即用的多语言支持**：内置简体中文与繁体中文翻译。
